import { Components, Theme } from "@mui/material";

export const ContainerStyle: Components<Theme> = {
  MuiContainer: {
    defaultProps: {
      maxWidth: 'lg',
    },
    styleOverrides: {
      root: {
        
      }
    }
  },
};
